services:
  postgres:
    ports:
      - "5433:5432"
  keycloak:
    ports:
      - "8088:8080"
  api:
    environment:
      SPRING_PROFILES_ACTIVE: dev
      STORAGE_UPLOAD_DIR: ./uploads
      STORAGE_BASE_URL: http://localhost:8081
    volumes:
      - ./uploads:/app/uploads
